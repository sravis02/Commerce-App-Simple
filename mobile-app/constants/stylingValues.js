import { getStatusBarHeight } from "react-native-status-bar-height";

export const spacing = {
  xs: 5,
  sm: 15,
  md: 20,
  lg: 25,
  xl: 30,
};

export const STATUS_BAR_HEIGHT = getStatusBarHeight();
export const ADDITIONAL_TOP_SPACE = spacing.md;
export const SPACING_HORIZONTAL = spacing.md;
export const SCREEN_BOTTOM_SPACE = 100;
